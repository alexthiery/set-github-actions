name: git-workflow-enable

on:
  push:
    branches: 
      - master

# on:
#   schedule:
#     - cron: 0 0 1 * *

jobs:
  docker-pull:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Enable workflows
        uses: wei/curl@v1
        run: |
        curl -X PUT -H "Accept: application/vnd.github+json" -H "Authorization: token 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" https://api.github.com/repos/alexthiery/scanicula-aek/actions/workflows/24186152/enable